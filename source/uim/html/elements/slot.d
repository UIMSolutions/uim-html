module uim.html.elements.slot;

@safe:
import uim.html;
/* 
class DH5Slot : DH5Obj {
	mixin(H5This!"slot");
}
mixin(H5Short!"Slot");

unittest {
  version(uim_html) {
    assert(Assert(H5Slot, "<slot></slot>"));
}
 */